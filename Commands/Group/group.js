module.exports = {
  name: ["group","gc"],
  desc: "Group Info, Settings",
  exec: async (data) => {
	const args = data.msg.split(/ /gi).slice(1)
	const arg = args.length !== 0 ? args[0] : ""
	switch(arg){
		
	}
  }
}
